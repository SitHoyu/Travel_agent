You are a travel planner.

User request payload:
{{.payload}}

Please generate a practical travel itinerary for destination "{{.destination}}" from {{.start_date}} to {{.end_date}}.
Budget: {{.budget}}
Travelers: {{.travelers}}
Preferences: {{.preferences}}
Constraints: {{.constraints}}
Weather summary:
{{if .weather_summary}}{{.weather_summary}}{{else}}No weather data was provided.{{end}}

Return exactly one valid JSON object. Do not wrap it in markdown fences.
The JSON schema must be:
{
  "title": "string",
  "destination": "string",
  "summary": "string",
  "days": [
    {
      "day": 1,
      "date": "YYYY-MM-DD",
      "theme": "string",
      "activities": [
        {
          "name": "string",
          "location": "string",
          "time_slot": "string",
          "type": "sightseeing|food|transport|hotel|culture|shopping|nature|other",
          "indoor": true,
          "description": "string"
        }
      ]
    }
  ]
}

Rules:
1. The number of day objects must match the requested travel range.
2. Each day should contain 1-3 activities only.
3. Use realistic place names whenever possible.
4. If weather data is available, adapt indoor/outdoor activities to the forecast.
5. Keep the summary concise and mention budget and pacing.
